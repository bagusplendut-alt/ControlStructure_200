a = float(input("Masukkan nilai pertama: "))
b = float(input("Masukkan nilai kedua: "))
c = float(input("Masukkan nilai ketiga: "))

if a > b and a > c: 
    largest = a
    print ('nilai besar = ', largest)
elif b > a and b > c:
    largest = b
    print ('nilai besar = ', largest)
elif c > a and c > b:
    largest = c
    print ('nilai besar = ', largest)
else:
    print ('nilai besar')